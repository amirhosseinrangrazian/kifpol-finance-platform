<div dir="rtl">

# 🐧 KifPool - مدیریت مالی شخصی

<div align="center">

**یک اپلیکیشن مدیریت مالی شخصی هوشمند با پشتیبانی از هوش مصنوعی**


</div>

---

## 📋 فهرست مطالب

- [معرفی](#-معرفی)
- [امکانات](#-امکانات)
-
-
- [تنظیمات هوش مصنوعی](#-تنظیمات-هوش-مصنوعی)
- [ساختار پروژه](#-ساختار-پروژه)
- [API Endpoints](#-api-endpoints)

---

## 🎯 معرفی

**KifPool** یک اپلیکیشن وب فارسی برای مدیریت امور مالی شخصی است. این برنامه با استفاده از Django ساخته شده و از یک مدل هوش مصنوعی لوکال (Dorna AI / Llama) برای ارائه مشاوره‌های مالی هوشمند استفاده می‌کند.

### چرا KifPool؟

- 🇮🇷 **کاملا فارسی**: رابط کاربری و تمام متون به زبان فارسی
- 📅 **تقویم شمسی**: پشتیبانی کامل از تاریخ شمسی
- 🤖 **مشاور هوشمند**: دستیار مالی با هوش مصنوعی
- 🔒 **لوکال و امن**: اطلاعات شما روی سیستم خودتان ذخیره می‌شود
- 💰 **رایگان**: مدل AI لوکال، بدون نیاز به API پولی

---

## ✨ امکانات

### 📊 داشبورد
- نمای کلی از وضعیت مالی
- نمودار درآمد و هزینه
- آخرین تراکنش‌ها
- وضعیت اهداف مالی

### 💳 مدیریت تراکنش‌ها
- ثبت درآمد و هزینه
- دسته‌بندی تراکنش‌ها
- فیلتر و جستجو
- Export به فایل CSV
- Import از فایل CSV

### 📁 دسته‌بندی‌ها
- دسته‌بندی‌های پیش‌فرض
- ایجاد دسته‌بندی شخصی
- زیردسته‌بندی‌ها
- آیکون و رنگ سفارشی

### 💰 مدیریت بودجه
- تعیین سقف بودجه برای هر دسته
- هشدار مصرف بودجه
- بودجه ماهانه و هفتگی

### 🎯 اهداف مالی
- تعریف اهداف پس‌انداز
- پیگیری پیشرفت
- واریز به اهداف
- مشاوره AI برای رسیدن به هدف

### 🤖 مشاور هوشمند (Dorna AI)
- پاسخ به سوالات مالی
- تحلیل الگوی مخارج
- پیشنهادات پس‌انداز
- راهنمایی سرمایه‌گذاری

### 📈 گزارش‌گیری و آنالیتیکس
- نمودارهای تعاملی
- مقایسه ماهانه
- تحلیل روند هزینه‌ها
- گزارش دسته‌بندی‌ها

### 👤 پروفایل کاربری
- تم‌های مختلف (زیتونی، آبی، تمشکی، تیره)
- آپلود آواتار
- تنظیمات شخصی

---

## 📋 پیش‌نیازها

- Python 3.10+
- pip (Python package manager)
- یک مدل Llama لوکال روی پورت 8080 (اختیاری برای AI)

---

## 🚀 نصب و راه‌اندازی

### 1. کلون کردن پروژه

```bash
git clone https://github.com/your-username/kifpool.git
cd kifpool
```

### 2. ساخت محیط مجازی

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. نصب وابستگی‌ها

```bash
pip install django requests pillow jdatetime
```

### 4. اعمال Migrations

```bash
python manage.py migrate
```

### 5. ایجاد کاربر ادمین (اختیاری)

```bash
python manage.py createsuperuser
```

### 6. اجرای سرور

```bash
python manage.py runserver
```

برنامه روی آدرس http://127.0.0.1:8000 در دسترس خواهد بود.

---

## 🤖 تنظیمات هوش مصنوعی

KifPool از یک مدل Llama لوکال برای مشاوره مالی استفاده می‌کند.

### تنظیمات پیش‌فرض

در فایل `abd/settings.py`:

```python
# Local Llama AI Settings
LLAMA_API_URL = 'http://localhost:8080/v1/chat/completions'
LLAMA_API_SECRET = 'kifpool-secret'
```

### راه‌اندازی مدل Llama

اگر از llama.cpp استفاده می‌کنید:

```bash
./server -m your-model.gguf --port 8080 --api-key kifpool-secret
```

### بدون AI

اگر مدل AI ندارید، برنامه بدون مشکل کار می‌کند. فقط قابلیت مشاور هوشمند غیرفعال می‌شود.

---

## 📁 ساختار پروژه

```
kifpool/
├── abd/                    # تنظیمات اصلی Django
│   ├── settings.py         # تنظیمات پروژه
│   ├── urls.py             # URLهای اصلی
│   └── wsgi.py
│
├── core/                   # اپلیکیشن اصلی
│   ├── models.py           # مدل‌های دیتابیس
│   ├── views.py            # ویوها
│   ├── urls.py             # URLها
│   ├── forms.py            # فرم‌ها
│   ├── admin.py            # پنل ادمین
│   └── services/
│       └── llama_service.py # سرویس هوش مصنوعی
│
├── templates/              # قالب‌های HTML
│   ├── base.html
│   ├── registration/
│   └── core/
│       ├── dashboard.html
│       ├── transactions.html
│       ├── analytics.html
│       ├── budget.html
│       ├── goals.html
│       ├── advisor.html
│       └── ...
│
├── static/                 # فایل‌های استاتیک
│   ├── css/
│   └── js/
│
├── media/                  # فایل‌های آپلود شده
│
├── db.sqlite3              # دیتابیس SQLite
├── manage.py
└── README.md
```

---

## 🔌 API Endpoints

### تراکنش‌ها
| Method | Endpoint | توضیحات |
|--------|----------|---------|
| GET | `/transactions/` | لیست تراکنش‌ها |
| POST | `/transactions/add/` | افزودن تراکنش |
| GET | `/transactions/export/` | خروجی CSV |
| POST | `/transactions/import/` | ورود از CSV |

### بودجه
| Method | Endpoint | توضیحات |
|--------|----------|---------|
| GET | `/budget/` | مدیریت بودجه |
| POST | `/budget/save/` | ذخیره بودجه |
| POST | `/budget/delete/<id>/` | حذف بودجه |

### اهداف
| Method | Endpoint | توضیحات |
|--------|----------|---------|
| GET | `/goals/` | لیست اهداف |
| POST | `/goals/add/` | افزودن هدف |
| POST | `/goals/<id>/deposit/` | واریز به هدف |
| GET | `/goals/<id>/advice/` | مشاوره AI |

### مشاور AI
| Method | Endpoint | توضیحات |
|--------|----------|---------|
| GET | `/advisor/` | صفحه مشاور |
| POST | `/advisor/ask/` | ارسال سوال |

### آنالیتیکس
| Method | Endpoint | توضیحات |
|--------|----------|---------|
| GET | `/analytics/` | صفحه گزارشات |
| GET | `/api/analytics-data/` | داده‌های نمودار |

---

## 🎨 تم‌ها

KifPool از 4 تم مختلف پشتیبانی می‌کند:

| تم | توضیحات |
|----|---------|
| 🫒 زیتونی | تم پیش‌فرض با رنگ سبز زیتونی |
| 🔵 آبی | تم آبی آرامش‌بخش |
| 🍇 تمشکی | تم صورتی شاد |
| 🌙 تیره | تم تاریک برای استفاده شبانه |



